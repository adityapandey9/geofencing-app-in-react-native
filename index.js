/**
 * @format
 * @lint-ignore-every XPLATJSCOPYRIGHT1
 */

import { AppRegistry } from "react-native";
import MainApp from "./MainApp";

AppRegistry.registerComponent("LocationTracking", () => MainApp);
